# --- Core backend (Phase 2) ---
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
python-dotenv>=1.0

# --- ML core (Phase 3) ---
numpy>=1.26
pandas>=2.2
scikit-learn>=1.4
joblib>=1.3

# --- Security (Phase 6) ---
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
slowapi>=0.1.9
cryptography>=42.0

# --- Testing (Phase 8) ---
pytest>=8.0
pytest-cov>=4.1
httpx>=0.27

# --- Security auditing (Phase 6) ---
pip-audit>=2.7
bandit>=1.7

# --- Optional extras -------------------------------------------------------
# CogniDiff degrades gracefully without these: every module that uses them has
# a documented NumPy fallback, so the API and dashboard run on the core set
# alone. Install them to enable the full research pipeline.
#
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install shap xgboost flwr anthropic
